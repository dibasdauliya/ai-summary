// Main exports
export { AISummaryWidget, createAISummary } from './AISummaryWidget';

// Types
export type { 
  AISummaryConfig, 
  AISummaryStyles, 
  AIService, 
  CreateAISummaryOptions 
} from './types';

// Icons (for custom usage)
export { 
  chatgptIcon, 
  claudeIcon, 
  geminiIcon, 
  perplexityIcon, 
  grokIcon, 
  icons 
} from './icons';

// Defaults (for customization)
export { DEFAULT_SERVICES, DEFAULT_QUERY, DEFAULT_TITLE } from './defaults';
